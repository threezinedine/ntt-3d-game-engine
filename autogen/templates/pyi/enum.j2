{% for enum in parser.Enums-%}
{% if "binding" in enum.annotations-%}
{{ enum.name }}: TypeAlias = int

{% for constant in enum.constants-%}
{% if "hidden" not in constant.annotations-%}
{{constant.name}} = {{constant.value}}
{% endif-%}
{% endfor-%}

{%- endif %}
{% endfor-%}